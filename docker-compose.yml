version: "3.9"

services:
  postgres:
    image: postgres:15-alpine
    container_name: postgres
    environment:
      POSTGRES_USER: escala365
      POSTGRES_PASSWORD: ${DB_PASSWORD:-escala365password}
      POSTGRES_DB: escala365
      POSTGRES_INITDB_ARGS: "-c max_connections=200"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U escala365"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - escala365-net

  redis:
    image: redis:7-alpine
    container_name: escala365-redis
    networks:
      - escala365-net

  asterisk:
    build:
      context: ./asterisk
      dockerfile: Dockerfile
    container_name: escala365-asterisk
    environment:
      AMI_PASSWORD: ${AMI_PASSWORD}
    volumes:
      - asterisk_data:/var/lib/asterisk
    ports:
      - "5060:5060/udp"
      - "5038:5038"
      - "8088:8088"
    networks:
      - escala365-net

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: escala365-backend
    environment:
      NODE_ENV: production
      DATABASE_URL: postgresql://escala365:${DB_PASSWORD}@postgres:5432/escala365_db
      REDIS_URL: redis://redis:6379
      ASTERISK_HOST: asterisk
      ASTERISK_PORT: 5038
      ASTERISK_USER: admin
      ASTERISK_PASSWORD: ${AMI_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
      PORT: 3000
    ports:
      - "3000:3000"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - escala365-net
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: escala365-frontend
    environment:
      REACT_APP_API_URL: http://localhost:3000
    ports:
      - "3001:3000"
    depends_on:
      - backend
    networks:
      - escala365-net

  nginx:
    image: nginx:alpine
    container_name: escala365-nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - backend
      - frontend
    networks:
      - escala365-net

volumes:
  postgres_data:
  asterisk_data:

networks:
  escala365-net:
    driver: bridge
