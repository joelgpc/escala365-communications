version: "3.9"

services:
  postgres:
    image: postgres:15-alpine
    container_name: postgres
    environment:
      POSTGRES_USER: escala365
      # Usamos la variable para asegurar que coincida con el backend
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      # Corregido: Debe coincidir con lo que busca el backend (escala365_db)
      POSTGRES_DB: escala365_db
      POSTGRES_INITDB_ARGS: "-c max_connections=200"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      # El usuario debe coincidir con POSTGRES_USER
      test: ["CMD-SHELL", "pg_isready -U escala365 -d escala365_db"]
      interval: 10s
      timeout: 5s
      retries: 5
      # Importante: Da 30 segundos de gracia al inicio
      start_period: 30s
    networks:
      - escala365-net

  redis:
    image: redis:7-alpine
    container_name: escala365-redis
    networks:
      - escala365-net

  asterisk:
    build:
      context: ./asterisk
      dockerfile: Dockerfile
    container_name: escala365-asterisk
    environment:
      AMI_PASSWORD: ${AMI_PASSWORD}
    volumes:
      - asterisk_data:/var/lib/asterisk
    ports:
      - "5060:5060/udp"
      - "5038:5038"
      - "8088:8088"
    networks:
      - escala365-net

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: escala365-backend
    environment:
      NODE_ENV: production
      # La URL ahora usa las variables correctas
      DATABASE_URL: postgresql://escala365:${DB_PASSWORD}@postgres:5432/escala365_db
      REDIS_URL: redis://redis:6379
      ASTERISK_HOST: asterisk
      ASTERISK_PORT: 5038
      ASTERISK_USER: admin
      ASTERISK_PASSWORD: ${AMI_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
      PORT: 3000
    ports:
      - "3000:3000"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - escala365-net
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: escala365-frontend
    environment:
      # Si el navegador del cliente debe acceder, esto debe ser la URL pública, no localhost
      # Si es renderizado servidor, localhost está bien. Asumo cliente:
      REACT_APP_API_URL: /api 
    ports:
      - "3001:80" # Nginx interno del frontend suele exponer 80
    depends_on:
      - backend
    networks:
      - escala365-net

  nginx:
    image: nginx:alpine
    container_name: escala365-nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - backend
      - frontend
    networks:
      - escala365-net

volumes:
  postgres_data:
  asterisk_data:

networks:
  escala365-net:
    driver: bridge
